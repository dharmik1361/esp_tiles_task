import 'package:flutter/material.dart';
import '../widgets/design_strip_widget.dart';

class DesignStripScreen extends StatelessWidget {
  const DesignStripScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DesignStripWidget();
  }
}
